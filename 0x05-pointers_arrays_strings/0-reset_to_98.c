#include "main.h"
/*including main.h makes it scan main.h, & include all functions within*/
/*within main.h is a list of every function my functions will call*/
/* more headers goes there */
/**
 * reset_to_98 - description
 * @n: the input is a pointer ♥
 * Return: depends
 */
void reset_to_98(int *n)
{
	*n=98;
/*This pointer can be named whatever you want, so long as you keep the "*" */
	/*That asterik seems to denote it as a pointer.*/
/*all we did was assign the pointer a value, so...*/
/* maybe by singing '&varName' as argument, it made our pointer automatically point to varName*/
}
